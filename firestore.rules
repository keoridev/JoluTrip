rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Разрешить чтение коллекции locations для всех (для ленты)
    match /locations/{locationId} {
      allow read: if true;
      allow write: if false;
    }
  }
}
